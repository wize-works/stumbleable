{
    "name": "stumbleable-chrome-extension",
    "version": "1.0.0",
    "description": "Chrome extension for Stumbleable - discover amazing content with one click",
    "scripts": {
        "build": "npm run clean && npm run compile && npm run copy-assets",
        "compile": "tsc",
        "copy-assets": "node scripts/copy-assets.js",
        "clean": "node scripts/clean.js",
        "watch": "tsc --watch",
        "package": "npm run build && node scripts/package.js"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.268",
        "typescript": "^5.6.2"
    },
    "dependencies": {}
}